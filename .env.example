# 이 파일을 복사해서 .env 를 만든다:
#   Windows PowerShell:  Copy-Item .env.example .env
#   mac/Linux:           cp .env.example .env
#
# 모든 값에 기본값이 있으므로 .env 가 없어도 앱은 그냥 뜬다. 필요할 때만 채운다.
# 실제 비밀(.env)은 절대 커밋하지 않는다(.gitignore 가 막아준다).

APP_NAME=app
DEBUG=false
